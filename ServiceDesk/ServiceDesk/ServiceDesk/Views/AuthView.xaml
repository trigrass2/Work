<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ServiceDesk.Views.AuthView"
             xmlns:syncfusion="clr-namespace:Syncfusion.SfBusyIndicator.XForms;assembly=Syncfusion.SfBusyIndicator.XForms"
             BackgroundColor="Accent">
    <ContentPage.Content>        
        <Grid VerticalOptions="Center" Margin="5,0,5,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>               
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            
            <Entry x:Name="loginEntry" Grid.Row="0" Text="{Binding User.Login, Mode=TwoWay}" BackgroundColor="White"/>
            
            <Entry x:Name="passwdEntry" Grid.Row="1" Text="{Binding User.Password, Mode=TwoWay}" IsPassword="True" BackgroundColor="White"/>
            <Button Text="OK" Grid.Row="2" Command="{Binding OkCommand}"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>