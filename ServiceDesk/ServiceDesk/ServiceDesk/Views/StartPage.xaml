<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ServiceDesk.Views.StartPage"
             BackgroundColor="#464451">
    <ContentPage.Content>
        <Grid VerticalOptions="Center" Margin="5,0,5,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="70"/>
                <RowDefinition Height="70"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button x:Name="googleSignInButton" Grid.Row="0" Text="Google" 
                    Command="{Binding OkCommand}" CommandParameter="google"
                    TextColor="#464451" BackgroundColor="Accent"/>
            <Button x:Name="oneCSignInButton" Grid.Row="1" Text="1C" Command="{Binding OkCommand}" 
                    TextColor="#464451" BackgroundColor="Accent" CommandParameter="1c"/>
            <Button x:Name="localSignInButton" Grid.Row="2" Text="Local" Command="{Binding OkCommand}" 
                    TextColor="#464451" BackgroundColor="Accent" CommandParameter="Local"/>
        </Grid>
    </ContentPage.Content>    
</ContentPage>