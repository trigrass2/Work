<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ServiceDesk.Views.SelectedTaskPage"             
             xmlns:myentry="clr-namespace:ServiceDesk"
             BackgroundColor="Accent">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Label">
                <Setter Property="VerticalOptions" Value="Center"/>
                <Setter Property="TextColor" Value="#464451"/>
                <Setter Property="HorizontalTextAlignment" Value="Start"/>
                <Setter Property="Margin" Value="5,0,0,0"/>
                <Setter Property="FontSize" Value="Small"/>
                <Setter Property="HorizontalOptions" Value="StartAndExpand"/>
            </Style>            
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout>            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>                
                <Label x:Name="titleLable" Grid.Column="0" 
                               Text="{Binding ServiceDesk_TaskListView.Title}" FontAttributes="Bold" 
                               TextColor="#464451" FontSize="20" 
                               HorizontalTextAlignment="Start" Margin="3,5,3,3"
                               VerticalTextAlignment="Center"/>
                <Button x:Name="Edit" BackgroundColor="Accent" Image="baseline_create_black_18.png" FontSize="10" TextColor="#464451" 
                        Grid.Column="1" Command="{Binding GoToEdit}" HeightRequest="50" WidthRequest="50"/>
            </Grid>           

            <ScrollView>
                <StackLayout VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" Spacing="0">                                                              
                    <Label x:Name="textTaskLabel" Text="{Binding ServiceDesk_TaskListView.Text}" HorizontalOptions="FillAndExpand"/>
                    <Label Text="заявитель" TextColor="White"/>                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="70"/>
                        </Grid.ColumnDefinitions>
                        <Label x:Name="initiatorLabel" Grid.Column="0" Text="{Binding ServiceDesk_TaskListView.Initiator_name}"/>
                        <Button x:Name="callBtn" Grid.Column="1" Image="baseline_call_black_24.png" Command="{Binding CallCommand}" 
                                HorizontalOptions="End" BackgroundColor="White" HeightRequest="50" WidthRequest="50" CornerRadius="25"
                                Margin="0,0,10,0"/>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackLayout Spacing="0" Grid.Column="0" VerticalOptions="Center">
                            <Label Text="статус " TextColor="White"/>
                            <Label x:Name="nameStatus"  Text="{Binding ServiceDesk_TaskListView.Status_name}"/>
                        </StackLayout>
                        <Button Text="{Binding TextButton}" Grid.Column="1" 
                                Command="{Binding EditStatusCommand}" 
                                CommandParameter="{Binding Source={x:Reference Name=nameStatus}, Path=Text}"
                                Margin="0,15,10,15" CornerRadius="20"
                                IsVisible="{Binding IsVisibleStatusButton}"
                                BackgroundColor="White" Padding="1"/>
                    </Grid>

                    <Label Text="завод" TextColor="White" IsVisible="{Binding IsVisibleFactory}"/>
                    <Label x:Name="factoryLabel" Text="{Binding ServiceDesk_TaskListView.Factory_name}" IsVisible="{Binding IsVisibleFactory}"/>

                    <Label Text="линия " TextColor="White" IsVisible="{Binding IsVisiblePlant}"/>
                    <Label x:Name="plantLabel" Text="{Binding ServiceDesk_TaskListView.Plant_name}" IsVisible="{Binding IsVisiblePlant}"/>

                    <Label Text="производственная единица" TextColor="White" IsVisible="{Binding IsVisibleUnit}"/>
                    <Label x:Name="unitLabel" Text="{Binding ServiceDesk_TaskListView.Unit_name}" IsVisible="{Binding IsVisibleUnit}"/>

                    <ListView IsVisible="{Binding IsVisibleAttach}" BackgroundColor="Accent" 
                              ItemsSource="{Binding Attachments}" SelectedItem="{Binding SelectedAttachment, Mode=TwoWay}" 
                              VerticalOptions="Start" HasUnevenRows="False" RowHeight="30" HeightRequest="100">
                        <ListView.Header>
                            <Label x:Name="titleAttach" Text="файлы" BackgroundColor="Accent" TextColor="White" HorizontalOptions="FillAndExpand"/>
                        </ListView.Header>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ViewCell>
                                    <ViewCell.View>
                                        <StackLayout>
                                            <Label Text="{Binding Attachment_name}" FontSize="13" TextColor="Blue"/>
                                        </StackLayout>
                                    </ViewCell.View>
                                </ViewCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <ListView x:Name="commentsListView"                              
                              ItemsSource="{Binding Comments}" 
                              HasUnevenRows="True"
                              SeparatorVisibility="None"
                              SelectionMode="None">
                        <ListView.Header>
                            <Label x:Name="titleComment" Text="Комментарии" BackgroundColor="#464451" TextColor="Accent" Margin="0" HorizontalOptions="FillAndExpand"/>
                        </ListView.Header>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ViewCell>
                                    <ViewCell.View>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Label x:Name="timeCreateCommentLable" Grid.Column="0" Grid.Row="0" Text="{Binding Timestamp}"/>
                                            <Frame Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" 
                                                   HorizontalOptions="FillAndExpand" VerticalOptions="StartAndExpand" 
                                                   BackgroundColor="#464451"
                                                   CornerRadius="7">
                                                <StackLayout>
                                                    <Label x:Name="nameCommentatorLabel" TextColor="Accent" Text="{Binding Commentator_name}"/>
                                                    <Label x:Name="textCommentLabel"  TextColor="Accent" Text="{Binding Text}"/>
                                                </StackLayout>
                                            </Frame>
                                        </Grid>
                                    </ViewCell.View>
                                </ViewCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackLayout>
            </ScrollView>
            <Grid BackgroundColor="#464451" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="50"/>                    
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>
                <Button x:Name="addAttachment" Grid.Column="0" Grid.Row="0"
                        Image="baseline_attach_file_white_18.png" TextColor="Accent" Command="{Binding AddNewAttachmentCommand}"
                        BackgroundColor="#464451"/>
                <Frame CornerRadius="10" Grid.Column="1" Grid.Row="0" Margin="0,2,0,2" Padding="0">
                    <myentry:MyEntry x:Name="sentComment" FontSize="13" Placeholder="Комментарий..."
                                FontAttributes="Italic" BackgroundColor="White"
                                HorizontalTextAlignment="Start" Margin="0" VerticalOptions="Center"/>
                </Frame>
                
                <Button x:Name="sendButton" Grid.Column="2" Grid.Row="0"
                        Image="baseline_send_white_18.png" BackgroundColor="#464451" 
                        TextColor="Accent" HorizontalOptions="EndAndExpand" Command="{Binding AddNewCommentCommand}" 
                        BindingContext="{Binding}" CommandParameter="{Binding Source={x:Reference Name=sentComment}, Path=Text}"/>
            </Grid>
        </StackLayout>        
    </ContentPage.Content>
</ContentPage>