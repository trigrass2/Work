<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Vertical.Views.ManualPage"
             BackgroundColor="#fafafa">
    <ContentPage.Content>
        <StackLayout Spacing="5" Margin="10">
            <Button Text="Объекты" Style="{x:StaticResource MenuButtonStyle}" FontSize="Body" Padding="5" Command="{Binding GoToManualObjectsPageCommand}"/>
            <Button Text="Типы Объектов" Style="{x:StaticResource MenuButtonStyle}" FontSize="Body" Padding="5" Command="{Binding GoToManualPropertiesPageCommand}"/>
            <Button Text="Свойства" Style="{x:StaticResource MenuButtonStyle}" FontSize="Body" Padding="5" Command="{Binding GoToManualTypesObjectsPageCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>